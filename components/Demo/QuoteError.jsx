import React from 'react';

const QuoteError = () => (
  <div className="px-4 lg:px-8 py-2">
    <p className="text-normal text-red-700 font-semibold">Random quote cannot be shown right now</p>
  </div>
);

export default QuoteError;
